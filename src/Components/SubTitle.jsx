import React from 'react'

const SubTitle = ({title}) => {
  return (
    <div>
        <h2 className='flex pt-3 justify-center text-gray-700 tracking-widest capitalize'> {title}</h2>
    </div>
  )
}

export default SubTitle